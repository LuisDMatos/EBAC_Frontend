function multiplicacao(a: number, b: number): number {
    return a * b;
}

const resultado = multiplicacao(10, 15);
console.log(resultado);



function dizOla(nome: string): string {
    return "Olá " + nome;
}

const saudacao = dizOla("Luis");
console.log(saudacao);
